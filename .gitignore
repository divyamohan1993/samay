# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/

# Raw per-instance run data is large + regenerable; keep it out of git.
# Curated deliverables (figures, summary tables, case results) live in artifacts/
# and ARE committed (the paper cites them).
runs/
*.log

# OS
.DS_Store
Thumbs.db

# Secrets — never commit
.env
*.pem
*.key

# browser test artifacts
.playwright-mcp/
*.jpeg
